<div class="container full-width rounded full-height">
    <div class="row">
        <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
                <p><a href="/commitee">&larr; back to the commitee</a></p>
                <hr>
                {{#if data.image.exists}}
                <img class="profile-picture" src="{{data.image}}"/>
                {{/if}}
                <h1 class="title name">{{data.member.name.full}}</h1>
                </header>
                <div class="description">
                    {{{data.member.description}}}
                </div>
            </article>
        </div>
    </div>
</div>
